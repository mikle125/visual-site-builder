# Beta Site Builder

Веб-приложение для создания простых сайтов с помощью drag-and-drop интерфейса. Позволяет создавать макеты страниц, добавлять элементы и экспортировать результат в виде HTML/CSS/JS файлов.

## 🌟 Основные возможности

- **Drag-and-drop** интерфейс для добавления элементов
- **Пять типов элементов**: контейнеры, заголовки, текст, кнопки, изображения
- **Редактирование свойств** в реальном времени
- **Предпросмотр** в разных разрешениях (десктоп, планшет, мобильный)
- **Экспорт** в ZIP архив с полным кодом сайта
- **Тёмная/светлая тема**
- **История действий** (отмена/повтор)
- **Локальное сохранение** проектов
- **Серверное сохранение** через Node.js сервер

## 🏗️ Структура проекта

```
├── index.html              # Главный HTML файл
├── styles.css             # Основные стили
├── state.js              # Менеджер состояния приложения
├── editor.js             # Основной редактор
├── property-manager.js   # Менеджер свойств элементов
├── canvas-manager.js     # Управление холстом
├── export.js             # Экспорт проектов
├── server.js             # Сервер для сохранения проектов (Node.js)
├── package.json          # Конфигурация Node.js проекта
├── README.md             # Документация
└── .gitignore            # Файлы для исключения из Git
```

## 🚀 Установка и запуск через Node.js

### 1. Установка Node.js

#### Windows:
1. Скачайте Node.js с [официального сайта](https://nodejs.org/)
2. Запустите установщик и следуйте инструкциям
3. Проверьте установку:
```cmd
node --version
npm --version
```

#### macOS:
```bash
# Используя Homebrew
brew install node

# Или скачайте с официального сайта
```

#### Linux (Ubuntu/Debian):
```bash
sudo apt update
sudo apt install nodejs npm
```

### 2. Установка зависимостей проекта

Клонируйте или скачайте проект, затем установите зависимости:

```bash
# Перейдите в папку проекта
cd beta-site-builder

# Установите зависимости Express.js
npm install express
```

Если у вас есть файл `package.json` с содержимым:
```json
{
  "name": "beta-site-builder",
  "version": "1.0.0",
  "description": "Drag-and-drop website builder",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
```

Тогда установите зависимости:
```bash
npm install
```

### 3. Запуск сервера

#### Способ 1: Запуск через npm
```bash
npm start
# Или для разработки
npm run dev
```

#### Способ 2: Прямой запуск
```bash
node server.js
```

#### Способ 3: Запуск с указанием порта
```bash
PORT=8080 node server.js
```

### 4. Доступ к приложению

После запуска сервера откройте браузер и перейдите по адресу:

```
http://localhost:3000
```

Если порт 3000 занят, сервер автоматически сообщит, на каком порту он запущен.

## 📦 Конфигурация сервера

Сервер использует Express.js и настраивается через файл `server.js`:

```javascript
const express = require('express');
const app = express();
const port = process.env.PORT || 3000;

// Статические файлы
app.use(express.static('.'));
app.use(express.json());

// API endpoints
app.post('/api/project/save', ...);
app.get('/api/project/load/:id', ...);
app.get('/api/project/list', ...);

app.listen(port, () => {
    console.log(`Сервер запущен на http://localhost:${port}`);
});
```

## 🎮 Использование приложения

### Основные функции:

1. **Добавление элементов**: Перетащите компонент из левой панели на холст
2. **Редактирование**: Выберите элемент и измените его свойства в правой панели
3. **Управление**: 
   - Перемещение: перетащите элемент
   - Изменение размера: потяните за углы
   - Удаление: кнопка "Удалить" или клавиша `Delete`
4. **Сохранение**: 
   - Локально: кнопка "Сохранить" (в LocalStorage)
   - На сервер: через серверное API (требует запущенного Node.js сервера)
5. **Экспорт**: Кнопка "Экспорт" создает ZIP архив с кодом сайта

### Серверное API:

- `POST /api/project/save` - сохранение проекта
- `GET /api/project/load/:id` - загрузка проекта
- `GET /api/project/list` - список проектов
- `POST /api/export` - экспорт проекта

## 🔧 Разработка

### Структура проекта:

```
state.js              # Управление состоянием приложения
editor.js             # Основная логика редактора
property-manager.js   # Управление свойствами элементов
canvas-manager.js     # Визуальное представление холста
export.js             # Логика экспорта в ZIP
server.js             # Node.js сервер для API
```

### Запуск для разработки:

```bash
# 1. Установите зависимости
npm install

# 2. Запустите сервер в режиме разработки
npm run dev

# 3. Откройте http://localhost:3000
```

### Тестирование API:

```bash
# Сохранение проекта
curl -X POST http://localhost:3000/api/project/save \
  -H "Content-Type: application/json" \
  -d '{"name":"Тестовый проект","data":"{}"}'

# Получение списка проектов
curl http://localhost:3000/api/project/list
```

## 🐛 Решение проблем

### Проблема: "Порт уже используется"
```bash
# Убейте процесс на порту 3000
# Linux/Mac:
lsof -ti:3000 | xargs kill -9

# Windows:
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# Или запустите на другом порту
PORT=4000 node server.js
```

### Проблема: "Ошибка модуля"
```bash
# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install
```

### Проблема: "Сервер не запускается"
1. Проверьте версию Node.js (требуется 14+)
```bash
node --version
```

2. Проверьте наличие файла server.js
3. Проверьте права доступа к файлам

## 🌐 Развертывание на хостинге

### Heroku:
```bash
heroku create your-app-name
git push heroku main
heroku open
```

### Vercel:
```bash
npm i -g vercel
vercel
```

### DigitalOcean:
```bash
# Установите PM2 для управления процессом
npm install -g pm2
pm2 start server.js --name "site-builder"
pm2 startup
pm2 save
```

## 📝 Скрипты package.json

```json
{
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm run build && npm start"
  }
}
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для изменений
3. Установите зависимости
4. Запустите сервер для тестирования
5. Создайте Pull Request

## 📧 Поддержка

По вопросам и проблемам:
1. Проверьте логи сервера в терминале
2. Проверьте консоль браузера (F12)
3. Создайте Issue в репозитории проекта

---

**Примечание**: Для полной функциональности (сохранение проектов на сервере) требуется запущенный Node.js сервер. Локальное сохранение в браузере работает без сервера.