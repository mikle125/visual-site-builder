# Beta Site Builder

Веб-приложение для создания простых сайтов с помощью drag-and-drop интерфейса. Позволяет создавать макеты страниц, добавлять элементы и экспортировать результат в виде HTML/CSS/JS файлов.

## 🌟 Основные возможности

- **Drag-and-drop** интерфейс для добавления элементов
- **Пять типов элементов**: контейнеры, заголовки, текст, кнопки, изображения
- **Редактирование свойств** в реальном времени
- **Предпросмотр** в разных разрешениях (десктоп, планшет, мобильный)
- **Экспорт** в ZIP архив с полным кодом сайта
- **Тёмная/светлая тема**
- **История действий** (отмена/повтор)
- **Локальное сохранение** проектов

## 🏗️ Структура проекта

```
├── index.html              # Главный HTML файл
├── styles.css             # Основные стили
├── state.js              # Менеджер состояния приложения
├── editor.js             # Основной редактор
├── property-manager.js   # Менеджер свойств элементов
├── canvas-manager.js     # Управление холстом
├── export.js             # Экспорт проектов
├── server.js             # Сервер для сохранения проектов (Node.js)
└── README.md             # Документация
```

## 🚀 Быстрый запуск

### Вариант 1: Статический сервер (рекомендуется)

1. **Установите Node.js** (если не установлен):
   - [Скачать с официального сайта](https://nodejs.org/)

2. **Установите простой HTTP-сервер**:
   ```bash
   npm install -g http-server
   ```

3. **Запустите проект**:
   ```bash
   http-server -p 3000
   ```

4. **Откройте в браузере**:
   ```
   http://localhost:3000
   ```

### Вариант 2: Live Server в VS Code

1. **Установите расширение Live Server** в VS Code
2. **Откройте папку проекта** в VS Code
3. **Нажмите "Go Live"** в правом нижнем углу
4. **Проект откроется** на `http://localhost:5500`

### Вариант 3: Python сервер

#### Для Windows:
```cmd
python -m http.server 8000
```

#### Для macOS/Linux:
```bash
python3 -m http.server 8000
```

### Вариант 4: PHP сервер
```bash
php -S localhost:8080
```

## 💻 Подробная установка по платформам

### Windows

#### Способ 1: Использование Node.js
```powershell
# 1. Скачайте и установите Node.js с https://nodejs.org/
# 2. Проверьте установку
node --version
npm --version

# 3. Установите http-server
npm install -g http-server

# 4. Перейдите в папку проекта
cd путь\к\вашему\проекту

# 5. Запустите сервер
http-server -p 3000 -c-1
```

#### Способ 2: Использование Python (есть в Windows 10+)
```cmd
# 1. Проверьте установлен ли Python
python --version

# 2. Если установлен, перейдите в папку проекта
cd путь\к\вашему\проекту

# 3. Запустите сервер
python -m http.server 8000
```

### macOS

#### Способ 1: Использование Node.js
```bash
# 1. Установите Node.js через Homebrew
brew install node

# 2. Установите http-server
npm install -g http-server

# 3. Перейдите в папку проекта
cd /путь/к/вашему/проекту

# 4. Запустите сервер
http-server -p 3000
```

#### Способ 2: Использование Python
```bash
# 1. Python уже установлен в macOS
# 2. Перейдите в папку проекта
cd /путь/к/вашему/проекту

# 3. Запустите сервер
python3 -m http.server 8000
```

### Linux (Ubuntu/Debian)

#### Способ 1: Использование Node.js
```bash
# 1. Установите Node.js
sudo apt update
sudo apt install nodejs npm

# 2. Установите http-server
sudo npm install -g http-server

# 3. Перейдите в папку проекта
cd /путь/к/вашему/проекту

# 4. Запустите сервер
http-server -p 3000
```

#### Способ 2: Использование Python
```bash
# 1. Установите Python (если не установлен)
sudo apt update
sudo apt install python3

# 2. Перейдите в папку проекта
cd /путь/к/вашему/проекту

# 3. Запустите сервер
python3 -m http.server 8000
```

## 🎮 Использование

### 1. Создание элементов
- Перетащите нужный компонент из левой панели на холст
- Доступные элементы:
  - **Контейнер** - для группировки элементов
  - **Заголовок** - текстовый заголовок (h1-h6)
  - **Текст** - абзац текста
  - **Кнопка** - интерактивная кнопка
  - **Изображение** - картинка по URL

### 2. Редактирование свойств
1. **Выберите элемент** на холсте
2. **Измените свойства** в правой панели:
   - Текст/URL изображения
   - Размер и положение
   - Цвета и шрифты
   - Отступы и скругления

### 3. Управление элементами
- **Перемещение**: перетащите элемент мышкой
- **Изменение размера**: потяните за уголки выбранного элемента
- **Удаление**: кнопка "Удалить" или клавиша `Delete`
- **Дублирование**: кнопка "Дублировать"
- **Редактирование текста**: двойной клик по элементу

### 4. Предпросмотр
- Нажмите кнопку **"Предпросмотр"** для открытия в модальном окне
- Используйте **кнопки в верхней панели** для переключения между разрешениями

### 5. Экспорт проекта
1. Нажмите кнопку **"Экспорт"**
2. Проект скачается в виде ZIP архива
3. Архив содержит:
   - `index.html` - HTML код страницы
   - `css/styles.css` - все стили
   - `js/scripts.js` - JavaScript код

## ⌨️ Горячие клавиши

| Комбинация | Действие |
|------------|----------|
| `Ctrl+Z` | Отменить |
| `Ctrl+Y` | Повторить |
| `Delete` | Удалить выбранный элемент |
| `Ctrl+S` | Сохранить проект |

## 🗄️ Сохранение проектов

### Локальное сохранение
- Нажмите **"Сохранить"** для сохранения в LocalStorage браузера
- Нажмите **"Загрузить"** для загрузки из LocalStorage

### Серверное сохранение (опционально)
Для использования серверного сохранения:

1. **Убедитесь, что Node.js установлен**
2. **Запустите сервер**:
   ```bash
   node server.js
   ```
3. **Проекты будут сохраняться** в папку `projects/`

## 🎨 Темы

Приложение поддерживает две темы:
- **Светлая** (по умолчанию)
- **Тёмная**

**Переключение**: нажмите на переключатель темы в правом верхнем углу

## 🔧 Технические особенности

### Оптимизации производительности:
- **Виртуальный DOM** для управления элементами холста
- **Дебаунс** для обновления свойств
- **Пул элементов** для переиспользования DOM узлов
- **Батчинг обновлений** состояния

### Архитектура:
- **Модульная структура** ES6
- **Централизованное состояние** (State Manager)
- **Разделение ответственности** между компонентами
- **Подписка на изменения** (Observer pattern)

## 🐛 Решение проблем

### Проблема: "Файлы не загружаются"
**Решение:**
1. Убедитесь, что вы запустили HTTP-сервер
2. Проверьте, что все файлы находятся в одной папке
3. Откройте консоль разработчика (F12) для просмотра ошибок

### Проблема: "Drag-and-drop не работает"
**Решение:**
1. Обновите страницу
2. Проверьте, не блокирует ли браузер JavaScript
3. Попробуйте другой браузер

### Проблема: "Экспорт не работает"
**Решение:**
1. Проверьте подключение к интернету (нужен доступ к CDN)
2. Обновите страницу
3. Проверьте разрешения браузера на скачивание файлов

## 🌐 Поддерживаемые браузеры

- **Chrome** 60+
- **Firefox** 55+
- **Safari** 11+
- **Edge** 79+

## 📝 Лицензия

Это учебный проект, созданный для демонстрации возможностей современного веб-разработки.

## 🤝 Вклад в проект

Если вы хотите улучшить проект:

1. Форкните репозиторий
2. Создайте ветку для ваших изменений
3. Внесите изменения
4. Создайте Pull Request

## 📧 Контакты

По вопросам и предложениям создавайте Issues в репозитории проекта.

---

**Примечание**: Это бета-версия проекта. Некоторые функции могут работать нестабильно.